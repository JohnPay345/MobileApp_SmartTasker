import { View, Text } from 'react-native'
import React from 'react'

export const ResetPasswordScreen = () => {
  return (
    // Будет объединён с экраном смены пароля
    <View>
      <Text>ResetPasswordScreen</Text>
    </View>
  )
}